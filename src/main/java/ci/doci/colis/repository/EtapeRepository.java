package ci.doci.colis.repository;

import ci.doci.colis.domaine.Etape;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EtapeRepository extends JpaRepository<Etape, Long> {
}
